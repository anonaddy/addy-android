<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/LightDarkMode"
    android:orientation="vertical"
    android:padding="@dimen/widget_margin">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/primaryColorStatic"
        android:padding="8dp">

        <ImageView
            android:id="@+id/widget_aliases_listview_list_open_app"
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:contentDescription="@string/app_name"
            android:src="@drawable/ic_logo" />

        <ImageView
            android:id="@+id/widget_aliases_listview_list_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_round_refresh_24"
            android:contentDescription="@string/refresh"
            app:tint="@android:color/white" />
    </RelativeLayout>


    <ListView
        android:id="@+id/widget_alias_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:loopViews="true" />

</LinearLayout>